<%- include("partials/title.ejs") %>

<body class="d-flex flex-column min-vh-100">
    <%- include("partials/header.ejs") %>

    <diV class="container py-3">

    <div class="d-flex">
        <div class="d-flex col-sm-6 justify-content-lg-start">
        <a href="/new-book"><div class="btn btn-secondary py-2">
            + New book
        </div></a>
        </div>  
        <div class="d-flex align-items-center justify-content-end col-sm-6 ps-3">
          <form action="/search-books" method="post">
            <div class="input-group mb-3">
              <input type="text" class="form-control" name="title" placeholder="Search title" aria-label="Title" aria-describedby="button-addon2" <% if (locals.search) { %>
               value="<%= search %>"
                <% } %>>
              <button class="btn btn-secondary" type="submit" id="button-addon2">Go</button>
            </div>
          </form>
        </div>
    </div>  
    
    <% if (locals.books) { %>        
        <div class="d-flex flex-column pt-3">
            <% books.forEach((book) => { %>

                <div class="card mb-3 shadow-sm rounded">
                <div class="row g-0">
                  <div class="col-lg-1">
                    <img src="<%= book.cover_url %>" class="rounded-start card-img img-thumbnail" alt="book cover">
                  </div>
                  <div class="col-lg-11 ps-4">
                    <div class="card-body">
                      <h5 class="card-title"><%= book.title %></h5>
                      <p class="card-text">by <%= book.author %></p>
                      <p class="card-text"><%= book.description %></p>
                      <div class="d-flex gap-2 align-items-center">
                      <div class="col-lg-6">
                        <p class="card-text"><small class="text-body-secondary">ISBN: <%= book.isbn %> </small></p>
                     </div>
                      <div class="d-flex justify-content-end col-lg-6 gap-2 pe-2">
                        <a href="/edit-book/<%= book.id %>" ><div class="btn btn-secondary">Edit</div></a>
                        <a href="/delete-book/<%= book.id %>" ><div class="btn btn-secondary">Delete</div></a>
                        <a href="/review/<%= book.id %>" ><div class="btn btn-secondary">Review</div></a>
                        <a href="/notes/<%= book.id %>" ><div class="btn btn-secondary">Notes</div></a>
                      </div>
                    </div>
                    </div>
                  </div>
                </div>
              </div>
                
            <% }); %>            
        </div>
    <% } %>   
</diV>

    <%- include("partials/footer.ejs") %>
</body>
</html>